import React from "react";

// import { Container } from './styles';

function Input({ label }) {
  return (
    <>
      <label>{label}</label>
      <input></input>
    </>
  );
}

export default Input;
